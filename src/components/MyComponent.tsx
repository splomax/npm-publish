import * as React from "react";
import {FC} from "react";
export const MyComponent: FC<any> = (props:any) => {
    return (
        <React.Fragment>
            <p>Hello Lib!</p>
        </React.Fragment>
    );
}